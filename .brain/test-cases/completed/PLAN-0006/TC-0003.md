# TC-0003 — Zero data loss + no stale references

**Plan:** PLAN-0006
**Status:** PASSED
**Purpose:** Every old file accounted for; no stale domain-path references outside allowlist.
**Related task:** T3, T5
**Related code/component:** git index + repo grep

## Preconditions
- Migration via git mv.

## Input
- Allowlist for old-path mentions: .brain/_deprecated/, update.sh + setup.sh migration/install code, .brain/planning/archived pre-lifecycle content, memory/decisions historical content.

## Steps
1. `git status --short` shows zero `D` (deleted) entries for tracked .brain files.
2. Grep repo (excluding .git, .claude worktrees, allowlist) for `.brain/(backend|frontend|devops|mobile|shared|brain)/` and `{domain}` path patterns, `memory/guidelines`, `memory/architecture`, `memory/tests/`, `memory/tasks/`.
3. Verify remaining hits are only allowlisted.

## Expected result
- Zero deletions; zero non-allowlisted stale references.

## Actual result
- PASSED. git status: 0 deletions (105 git-mv renames, 0 D entries). Repo-wide grep: stale refs fixed in constitution/RULES.md (R16-R20, R36-R40, R44), rules/git/safety.md, templates/summary/TASK_SUMMARY.md, skills/MEMORY.md, session-bus+sessions READMEs, knowledge pattern docs. Remaining hits allowlisted only (update.sh migration code, _deprecated/, archived history, INSTRUCTIONS.md old-behavior note, PLAN-0006 self-documentation).

## Execution
- **Date:** 2026-09-05
- **Executor/agent:** opencode agent
- **Notes:** git status: 0 deletions (105 git-mv renames, 0 D entries). Repo-wide grep: stale refs fixed in constitution/RULES.md (R16-R20, R36-R40, R44), rules/git/safety.md, templates/summary/TASK_SUMMARY.md, skills/MEMORY.md, session-bus+sessions READMEs, knowledge pattern docs. Remaining hits allowlisted only (update.sh migration code, _deprecated/, archived history, INSTRUCTIONS.md old-behavior note, PLAN-0006 self-documentation).

## Failure information
-
