# RAI-Engineering — Database connections (schema only, no secrets)
.brain/connections/

# Inter-session ephemeral data (message bus, live session registrations)
.brain/session-bus/
.brain/sessions/live/

.claude/
