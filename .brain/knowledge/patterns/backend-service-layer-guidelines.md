---
domains: [backend]
---

# Project Guidelines

> **Auto-generated by RAI-Engineering ARCHITECT agent.**
> **This file is the source of truth for project structure and conventions.**
> **Any AI tool reading this can understand the full project.**

---

## Architecture

```
Pattern: Service Layer + Repository
├── Controllers — thin, handle HTTP only (max 30 lines per method)
├── Services — business logic, stateless, testable, transactional
├── Models — Eloquent/ORM, relationships, scopes, UUIDs
└── API Resources — response transformation, field filtering
```

**Key patterns:**
- Controllers delegate to Services (never direct DB calls in controllers)
- Services use Repositories for data access
- Validation uses Form Request classes (not inline in controllers)
- Responses use API Resource classes (not returning models directly)

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Backend | Laravel |
| Database | MySQL / PostgreSQL |
| API | RESTful JSON API |
| Testing | PHPUnit / Pest |
| Auth | Sanctum (stateless tokens) |

---

## Custom Commands

Check `.ai/commands/` for custom artisan commands.

---

## Middleware

| Middleware | Scope |
|-----------|-------|
| `auth:sanctum` | `/api/v1/*` |
| `throttle:api` | `/api/v1/*` |

---

## Database Conventions

- **Engine:** InnoDB
- **Charset:** utf8mb4
- **Public IDs:** UUIDs (not auto-increment)
- **Soft deletes:** All user-facing data
- **Timestamps:** Every table has `created_at` and `updated_at`
- **Indexes:** Every foreign key + every WHERE/ORDER BY column
- **Naming:** `snake_case` columns, plural table names

## Routes

| Prefix | File |
|--------|------|
| `/api/v1/*` | `routes/api.php` |

Route naming: `api.v1.{resource}.{action}`

## Coding Standards

- **PHP:** PSR-12
- **Type hints:** Required on all methods
- **Return types:** Required on all methods
- **Validation:** Form Request classes
- **Responses:** API Resource classes
- **Naming:** PascalCase classes, camelCase methods, descriptive variables (R26)

## Brain

The project uses **RAI-Engineering** as its main engine. See `.brain/INDEX.md` for full map.

---

## Orchestration

RAI-Engineering v1.4+ includes an **Orchestration & Parallel Execution Engine** for any task spanning multiple domains or containing independent sub-tasks.

**How it works:**
1. BRAIN detects a multi-area or complex task and routes to ORCHESTRATOR ENGINE
2. ORCHESTRATOR ENGINE decomposes into sub-tasks with a dependency graph
3. Sub-tasks are dispatched in parallel waves (no-dependency tasks run together)
4. Cross-agent requests are relayed in real-time (never blocked > 1 cycle)
5. Conflicts are auto-resolved using project rules
6. A verification loop (max 3 cycles) checks contracts, todos, wiring
7. If gaps found, affected sub-agents are re-dispatched for fixes
8. After completion, normal REVIEWER → MEMORY SCRIBE → SUMMARY flow resumes

**Key agents:**
- `ORCHESTRATOR ENGINE` — Task orchestrator (decompose, dispatch, relay, verify)
- Each domain sub-agent (Backend, Frontend, etc.) receives well-scoped sub-tasks

**Rules:** R41-R45 govern orchestration behavior.
**Protocol:** `.brain/reference/orchestration-protocol.md` defines the full specification.
